name = "solar-strike-leaderboard"
main = "worker.js"
compatibility_date = "2026-05-01"

# KV namespace binding — created via `wrangler kv:namespace create SCORES`.
# Paste the returned id here.
[[kv_namespaces]]
binding = "SCORES"
id = "REPLACE_WITH_KV_ID"
